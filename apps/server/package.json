{
  "name": "server",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "qianfo server",
  "main": "index.ts",
  "types": "index.d.ts",
  "scripts": {
    "build": "bun build index.ts --outfile dist/index.js --format esm",
    "dev": "bun --watch index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
